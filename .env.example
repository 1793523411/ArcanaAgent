# Server port (default: 3001)
PORT=3001

# Data directory for conversations, config, and uploaded skills (default: ./server/data)
# DATA_DIR=/path/to/data

# API key for model provider (overrides config/models.json)
# VOLCENGINE_API_KEY=your-api-key-here
